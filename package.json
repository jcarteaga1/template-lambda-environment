{
  "name": "template-lambda-environment",
  "version": "1.0.0",
  "description": "example of how to configure development environments for a lambda function using github actions",
  "author": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "serverless": "^2.69.1"
  }
}
